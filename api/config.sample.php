<?php
/**
 * Скопируйте этот файл рядом под именем config.php и впишите свои значения.
 * config.php в репозиторий не попадает — пароль не должен лежать в открытом коде.
 */
return [
    // ── Куда приходят заявки ───────────────────────────────────
    'mail_to'   => 'soldout-loft@yandex.ru',

    // ── Ящик, от имени которого уходит письмо ──────────────────
    // Обычно тот же самый. Домен должен совпадать с SMTP-аккаунтом,
    // иначе письма уйдут в спам.
    'mail_from' => 'soldout-loft@yandex.ru',

    // ── SMTP ───────────────────────────────────────────────────
    // Для Яндекс.Почты: smtp.yandex.ru, порт 465, шифрование ssl.
    //
    // Пароль нужен НЕ обычный от почты, а пароль приложения:
    // id.yandex.ru → Безопасность → Пароли приложений → Почта.
    // Обычный пароль Яндекс для SMTP не принимает.
    'smtp_host' => 'smtp.yandex.ru',
    'smtp_port' => 465,
    'smtp_user' => 'soldout-loft@yandex.ru',
    'smtp_pass' => '',

    // Если SMTP настроить не получается, поставьте true — письма пойдут
    // через встроенную отправку хостинга. Проще, но чаще попадает в спам.
    'use_php_mail' => false,
];
